load(
    "//tools:defaults.bzl",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "canvas-core",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    assets = [],
    module_name = "@gradii/diagram/canvas-core",
    deps = [
        "@npm//lodash",
        "@npm//closest",
        "//src/diagram/geometry"
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
