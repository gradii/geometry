// Configuration for IDEs only.
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "..",
    "baseUrl": ".",
    "paths": {
      "@gradii/annotation": [
        "../annotation"
      ],
      "@gradii/check-type": [
        "../check-type"
      ]
    }
  },
  "include": [
    "./**/*.ts"
  ],
  "exclude": [
    "**/*.spec.ts"
  ]
}