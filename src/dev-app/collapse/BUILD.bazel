load("//tools:defaults.bzl", "ng_module", "sass_binary")

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "collapse",
    srcs = glob(["**/*.ts"], exclude = ["**/*.spec.ts"],),
    assets = [
        "dev-collapse.html",
        ":dev_collapse_scss",
    ],
    deps = [
        "//src/triangle/collapse",
        "@npm//@angular/forms",
        "@npm//@angular/router",
    ],
)

sass_binary(
    name = "dev_collapse_scss",
    src = "dev-collapse.scss",
)
