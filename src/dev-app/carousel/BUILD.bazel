load("//tools:defaults.bzl", "ng_module", "sass_binary")

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "carousel",
    srcs = glob(["**/*.ts"], exclude = ["**/*.spec.ts"],),
    assets = [
        "dev-carousel.html",
        ":dev_carousel_scss",
    ],
    deps = [
        "//src/triangle/carousel",
        "@npm//@angular/cdk",
        "@npm//@angular/router",
    ],
)

sass_binary(
    name = "dev_carousel_scss",
    src = "dev-carousel.scss",
)
