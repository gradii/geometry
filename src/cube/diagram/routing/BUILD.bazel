load(
    "//tools:defaults.bzl",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "routing",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    assets = [],
    module_name = "@gradii/diagram/routing",
    deps = [
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
