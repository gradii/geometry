load(
    "//tools:defaults.bzl",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "diagram-engine",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    assets = [],
    module_name = "@gradii/diagram/diagram-engine",
    deps = [
        "//src/diagram/diagram-core",
        "//src/diagram/defaults",
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
