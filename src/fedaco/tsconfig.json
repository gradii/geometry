// Configuration for IDEs only.
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "baseUrl": ".",
    "paths": {
      "@gradii/check-type": [
        "../check-type"
      ]
    }
  },
  "include": [
    "./**/*.ts"
  ]
}