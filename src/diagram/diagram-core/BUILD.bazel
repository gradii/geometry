load(
    "//tools:defaults.bzl",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "diagram-core",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    assets = [],
    module_name = "@gradii/diagram/diagram-core",
    deps = [
        "//src/diagram/canvas-core",
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
