load(
    "//tools:defaults.bzl",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "defaults",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    assets = [],
    module_name = "@gradii/diagram/defaults",
    deps = [
        "//src/diagram/diagram-core",
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
